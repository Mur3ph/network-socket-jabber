package main.ie.murph.network.hash;

import org.apache.log4j.LogManager;
import org.apache.log4j.Logger;

public class NetworkHashMap {
	private static final Logger LOGGER = LogManager.getLogger(NetworkHashMap.class.getName());
	public int hash = 31;

	public static void main() {
		LOGGER.info("++main()");
	}
}
